import { Text } from '@mantine/core';
import * as React from 'react';

interface Props {
  // someProps: string
}

export function DiscussionTab(props: Props) {
  // const { someProps } = props;

  return (
    <div className="p-6">
      <Text>Discussion Tab</Text>
    </div>
  );
}
